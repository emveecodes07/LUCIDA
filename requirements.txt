streamlit>=1.36
openai>=1.30
requests>=2.31
pypdf>=4.2
python-pptx>=0.6.23
python-docx>=1.1

# Emotion detection (auto stress detection). App falls back to a keyword
# heuristic if these aren't installed — install for the real DistilRoBERTa model.
transformers>=4.40
torch>=2.2
# ^ CPU-only machines: `pip install torch --index-url https://download.pytorch.org/whl/cpu`
#   then `pip install transformers` separately — much smaller download.

# Optional — only needed for mic voice input (app runs fine without these)
# streamlit-mic-recorder>=0.0.8
# faster-whisper>=1.0
